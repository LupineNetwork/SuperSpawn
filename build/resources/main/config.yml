mysql:
    driver: com.mysql.jdbc.Driver
    # Change this to your actual database url
    url: jdbc:mysql://localhost/test
    username: user
    password: secure
    
    # Where everything is stored
    table-name: superspawn

messages:
    must-be-player: "&cYou must be a player to execute this command!"
    no-such-player: "&cThe player {0} does not exist!"
    # Triggered when the player does not have the "superspawn.sendplayertospawn" node
    no-perms: "&cInsufficient permissions to do /spawn player!"